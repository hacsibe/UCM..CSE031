#ifndef waitinglist
#define waitinglist
#include <iostream>
#include <stack>
#include "StringQueue.h"

using namespace std;
string name = "/0";
char commnd="";
int main (int argc, const char * argv[]){

//	Queue queue;
//	while (commnd != 'q') 
{
/*

	cin >> commnd;


	if (commnd == 'a') {
	cin >> name;
	queue.push (name);
	cout << name + " arrived" << endl;

	}


	if (commnd == 'n') {
	   if (queue.isEmpty())
		 {
		cout << "No customers waiting" << endl;
		commnd='q';
		}

		else {   
		cout << "Next in line: " + queue.peek() << endl;
	   
		}
	}

	if (commnd == 's') {

        if (queue.isEmpty()) {
		cout << "No customers waiting" << endl;

	}
	else {
	  cout << "Now serving: " + queue.peek() << endl;
	  queue.pop();
    	} 
 	 }



	if (commnd == 'q' ) {
	
	
	if (queue.isEmpty()) {
		cout << "No customers waiting" << endl;
		break;

	}
	if (!queue.isEmpty()) {
			cout << "Come back tomorrow: " << endl;
			
	
      	 }

  	while (!queue.isEmpty()) {
			cout << queue.pop() << endl;
	}
	
 

	cout << "Closing up now." << endl;
*/
}
return 0;
}

#endif
